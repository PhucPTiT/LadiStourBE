services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ladi-stour-app
    environment:
      SPRING_APPLICATION_NAME: stour
      SPRING_MONGODB_URI: mongodb://root:rootpassword@103.89.94.201:27020/ladi_stour?authSource=admin
      SERVER_PORT: 8080
    ports:
      - "8080:8080"
